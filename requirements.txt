# DocMind AI — Python dependencies
# Install with: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Werkzeug==3.0.3

# ── Rate limiting ──────────────────────────────────────
Flask-Limiter==3.8.0

# ── Environment & configuration ───────────────────────
python-dotenv==1.0.1

# ── Document parsing ──────────────────────────────────
# IMPORTANT: use lowercase "pymupdf" (pre-built wheel, no compiler needed).
# Do NOT use "PyMuPDF" (builds from source, requires Visual Studio).
pymupdf>=1.24.0           # PDF parsing — import as "fitz"
python-docx==1.1.2        # DOCX parsing
python-pptx==0.6.23       # PPTX parsing

# ── IBM watsonx.ai SDK ────────────────────────────────
ibm-watsonx-ai>=1.1.2
ibm-cloud-sdk-core>=3.22.0

# ── Utilities ─────────────────────────────────────────
Pillow>=10.0.0
email-validator==2.2.0
WTForms==3.1.2

# ── Production server ─────────────────────────────────
# gunicorn is Linux/macOS only — not needed for local Windows dev.
# It is installed automatically on Railway / Render / Heroku.
gunicorn>=22.0.0          ; sys_platform != "win32"
